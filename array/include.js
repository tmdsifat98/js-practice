const books = ['math','physics','chemistry','biology'];

const result = books.includes('math'); //khuje ber kore array te element ache kina
console.log(result);

const check = Array.isArray(books);  //array ki na seta find out kore
console.log(check);